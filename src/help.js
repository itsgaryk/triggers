module.exports = {
    /*
    name: 'help',
    description: 'Ping!',
    args: false,
	execute(message, args, serverConfig) {
        client.commands.ForEach((r, indexValue) => {
            {r.name; r.description}
            if 
        })
        
    },
    */
};